#include <vector>
#include <Windows.h>
#include "UserInput.h"
#include "Team.h"

int main()
{
    SetConsoleCP(1251);
    SetConsoleOutputCP(1251);

    int choice;
    bool exit = false;
        
    while (!exit)
    {
        
    }

    return 0;
}
